import { NextPage } from 'next';

const ContractPage: NextPage = () => {
  return (
    <div
      className="container"
      style={{
        paddingTop: '120px',
      }}
    >
      <h2
        style={{ color: '#7A5AF8', fontWeight: '600', fontSize: '3.42rem', textAlign: 'center', marginBottom: '5px' }}
      >
        Ждет текст в фигме ;&#40;
      </h2>
    </div>
  );
};

export default ContractPage;
